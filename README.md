tools:SQLite,html,css,js,node.js
clone the repository into your machine open cmd and get over the backend folder with the cd command then start the app.js file with the command "node app.js" that's all !
